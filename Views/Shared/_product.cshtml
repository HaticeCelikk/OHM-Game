@model Game
<div class="card mb-2">
    <img class="card-img-top" src="~/img/@Model.ImageUrl" alt="">
    <div class="card-body">
        <h5 class="card-title">@Model.Name</h5>
        <p class="card-text">@Model.Description</p>	
        <a asp-controller = "Game" asp-action = "Details" asp-route-id = "@Model.GameId" class="btn btn-primary">incele</a>
    </div> 
</div>	